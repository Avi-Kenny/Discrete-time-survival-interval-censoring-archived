#' Impose missingness structure on dataset
#'
#' @param dataset A dataset returned by generate_dataset()
#' @return The same dataset but with missingness structure imposed

impose_missingness <- function(dataset) {
  
  # !!!!!
  
  return(dataset)
  
}
